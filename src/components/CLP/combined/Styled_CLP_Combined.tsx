import styled from "styled-components";

export const CLP_CombinedContainer_Styled = styled.div`
    .overlay{
      z-index: 12000000;
      position: fixed;
      background-color: rgba(0,0,0,0.25);
      inset: 0;
      backdrop-filter: grayscale(1) brightness(0.75) blur(2px);
    } 
    
`