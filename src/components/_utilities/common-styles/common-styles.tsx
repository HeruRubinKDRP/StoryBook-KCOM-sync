 export const absoluteStretch=( top: string, right:string, bottom:string, left:string)=>{
  return `
  position : absolute;
  top: ${top};
  left: ${left};
  right:${right};
  bottom: ${bottom};
  `
 }
